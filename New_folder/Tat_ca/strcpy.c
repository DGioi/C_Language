#include <stdio.h>
#include <string.h>

int main()
{  char c[5];
   char src[40];
   char dest[10];
   strcpy(src, "Hoc C co ban va nang cao tai VietJack !!!");
   puts(src);
   printf("\n");
   strcpy(dest, "Hoc C co ban va nang cao tai VietJack !!!");
   puts(src);
   printf("%d",scanf("%s",c));
   return(0);
}


